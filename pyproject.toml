[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "poppy"
version = "0.1.0"

[tool.pytest.ini_options]
pythonpath = [""]
testpaths = ["test"]
